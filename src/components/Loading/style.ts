import styled from 'styled-components'

export const Wrapper = styled.div`
  width: 100%;
  height: 100%;
  position: fixed;
  background-color: var(--gray100);
  z-index: 9999;
  top: 0;
  left: 0px;

  display: flex;
  align-items: center;
  justify-content: center;

  background-image: radial-gradient(circle 1278px at 49.9% 50%);
  opacity: 0.8;
`
